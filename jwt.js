/***
 * install jsonwebtoken
 * install cookie-parser
 * jwt.sign (payload,secret,{expiresIn:})
 * token client
 * **** */

/***
 * how to store token in the client side
 * 1. memory --> ok type
 * 2. local storage -->ok type (XSS)
 * cookies :http only
 * **** */

/****
 *1. set cookies with http only. for development secure: true
 *2.cors important: app.use(cors({
  origin:['http://localhost:5173/'],
  credentials:true,
})); 
 *3.client side axios settings
 * in axios set withCredentials:true
 */

 /****
  * 1. to send cookies from the client make sure you added credentials :true, for the api call using axios
  *3 .use cookie parser as middleware
 */

  /***
   * express from req get the url
   * 
   * 
   * 
   */

/**
 * important :
 * 1.Authentication and upload img , drag and drop (img,file)
 * 2.Authorization and jwt
 * 3.animation
 * 4.chart or compare 
 * 5.awesome-react: react admin ba B2B apps, react loading skeleton,google calendar , qr code,react complex tree, swiper.com,react hook from, react dashboard ui, react pdf and figma, bibinno vasha translate : (formatjs)
 * ** */
